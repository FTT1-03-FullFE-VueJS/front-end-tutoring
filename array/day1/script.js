/**
 * 1. Introduce
 * Một cấu trúc dữ liệu
 *
 * var listNumber:int = [1, 2, 3, 4, 5, 6];
 * var listString:string = ['string1', 'string2', 'string3'];
 *
 * - KN: Array là một cấu trúc dữ liệu dùng để lưu trữ các phần tử cùng loại (cùng kiểu dữ liệu)
 * - Để khai báo một array
 *   + b1: Xác định kiểu dữ liệu của array
 *   + b2: Đặt tên cho array
 *   + b3: Khai báo danh sách elements bên trong array, hoặc khai báo array là rỗng
 *
 * example: listAge:int = []
 * example: listAge:int = [1, 2, 3]
 */

/**
 * 2. Method 1
 * push(element): Thêm một phần tử vào đuôi của array
 * unshift(element): Thêm một phần tử vào đầu của array
 * pop(): Xóa một phần tử ở đuôi của array
 * shift(): Xóa một phần tử ở đầu của array
 */

/**
 * 3: Cập nhật một dữ liệu được chỉ định trong array
 */

/**
 * 4: Method 2
 * - find(callback) - tìm kiếm phần tử
 *  -> Nếu tìm kiếm có giá trị thì nó sẽ trả về giá trị mà ta tìm kiếm
 *  -> Nếu không tìm thấy thì nó sẽ trả về undefined
 *
 * - map(callback) - Cập nhật giá trị trong mãng
 * preValue -> map -> resultValue
 *
 * - filter(callback) - Lọc các element trong mãng
 *
 * - sort(callback) - sắp xếp mảng
 */
